package scalapuzzlers

/**
 * User: jconley
 * Date: 6/12/2014
 */
object Puzzle2 extends App{
  var MONTH = 12; var DAY = 24
//  var (HOUR, MINUTE, SECOND) = (12, 0, 0)

  /*
  This is because multiple variable assignments are based on pattern
matching, and within a pattern match, variables starting with an uppercase
letter take on a special meaning: they are stable identifiers.
   */
}
